# Model checkpoints

This directory contains trained model checkpoints used in the project. Each subfolder groups checkpoints by model family. For UMA, we don't store the checkpoints separately but use the `fairchem.core.pretrained_mlip` to load the models.
